import 'package:flutter/material.dart';

class AppWindowConfig {
  AppWindowConfig._(); // Prevent instantiation

  // Window dimensions
  static const double windowWidth = 697.0;
  static const double windowHeight = 407.0;
  
  // Window size
  static const Size windowSize = Size(windowWidth, windowHeight);
  
  // Window positioning
  static const bool centerWindow = true;
}